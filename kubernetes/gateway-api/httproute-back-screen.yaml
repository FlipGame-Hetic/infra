apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: back-screen-route
  namespace: game-system
  labels:
    app.kubernetes.io/name: back-screen
    app.kubernetes.io/part-of: flipper
    app.kubernetes.io/managed-by: kustomize
spec:
  parentRefs:
    - name: flipper-gateway
  hostnames:
    - "back.flipper.example.com"
  rules:
    - backendRefs:
        - name: back-screen
          port: 80
